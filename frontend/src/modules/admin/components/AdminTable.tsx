export function AdminTable({ columns, rows, category }: { columns: string[]; rows: string[][]; category: string }) {
  return <div className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm shadow-gray-100/60"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-gray-200 text-sm"><thead className="bg-gray-50"><tr>{columns.map((c) => <th key={c} className="px-4 py-3 text-left font-semibold text-gray-600">{c}</th>)}</tr></thead><tbody className="divide-y divide-gray-100 bg-white">{rows.map((row,idx) => <tr key={`${category}-${idx}`} className="hover:bg-gray-50/70">{row.map((cell,i) => <td key={i} className="px-4 py-3 text-gray-700">{cell}</td>)}</tr>)}</tbody></table></div></div>;
}
